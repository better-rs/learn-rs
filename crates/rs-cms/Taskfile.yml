version: "3"

tasks:
  init:
    - echo "init"

  install:
    - cargo install cargo-watch  # auto compile and run:

  run:
    cmds:
      - echo "run cms"
      - cargo run

  # auto compile and run:
  watch:
    - cargo watch -x run

  open:
    cmds:
      - open http://localhost:3000
